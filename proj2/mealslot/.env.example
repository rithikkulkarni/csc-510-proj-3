# Example env file — do NOT commit real secrets.
# Copy to .env and fill values locally when developing/building.
MAPS_API_KEY=YOUR_MAPS_API_KEY_HERE
YOUTUBE_API_KEY=YOUR_YOUTUBE_API_KEY_HERE
NEXT_PUBLIC_STACK_PROJECT_ID=your_stack_project_id
NEXT_PUBLIC_STACK_PUBLISHABLE_CLIENT_KEY=your_stack_publishable_key
# Database
DATABASE_URL="file:./dev.db"  # SQLite default for Codespaces

# Optional Postgres (used only if docker-compose up and you change DATABASE_URL)
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/mealslot?schema=public"

# Auth (optional; app falls back to MockAuth if missing)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
AUTH_SECRET="dev-secret-not-used-when-empty"

# External APIs (optional; stubs are used if empty)
OPENAI_API_KEY=""
YOUTUBE_API_KEY=""
MAPS_API_KEY=""
NEXT_PUBLIC_GOOGLE_MAPS_KEY="" # Same as above

# WS Server
WS_URL="http://localhost:4001"
