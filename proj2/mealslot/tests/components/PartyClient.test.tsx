import { describe, it, expect } from "vitest";

describe("PartyClient (placeholder)", () => {
  it("placeholder to avoid flaky heavy integration tests", () => {
    expect(true).toBe(true);
  });
});
